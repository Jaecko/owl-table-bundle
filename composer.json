{
    "name": "owl-concept/table-bundle",
    "description": "Bundle Symfony pour la génération dynamique de tableaux HTML avec tri, filtres et pagination (mode serveur & client) | Symfony bundle for dynamic HTML table generation with sorting, filtering and pagination (server & client mode)",
    "type": "symfony-bundle",
    "license": "proprietary",
    "require": {
        "php": ">=8.1",
        "symfony/framework-bundle": "^6.4 || ^7.0",
        "symfony/twig-bundle": "^6.4 || ^7.0",
        "twig/twig": "^3.0"
    },
    "autoload": {
        "psr-4": {
            "OwlConcept\\TableBundle\\": "src/"
        }
    },
    "extra": {
        "symfony": {
            "require": "^6.4 || ^7.0"
        }
    }
}
